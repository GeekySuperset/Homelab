# Copy to .env in this directory before `docker compose up`.
# Bootstrap creates shared networking and edge services.

COMPOSE_PROJECT_NAME=homelab-bootstrap

# Host IP used in Pi-hole local DNS host entries.
HOMELAB_HOST_IP=

# Pi-hole web UI and API password (also used by Homepage widget labels).
PIHOLE_WEB_PASSWORD=
